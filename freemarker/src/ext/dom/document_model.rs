//! 文档节点模型 —— 对应 Java `freemarker.ext.dom.DocumentModel`
//! （XML 文档节点的模型；Rust 侧由 `XmlNode` 的 Root 分支承载）

/// Java 类锚点：`DocumentModel`（Rust 侧由 `XmlNode` 的 Root 分支承载）
#[allow(dead_code)]
pub(crate) struct DocumentModel;
