//! 处理指令节点模型 —— 对应 Java `freemarker.ext.dom.PINodeModel`
//! （XML 处理指令节点的模型；Rust 侧由 `XmlNode` 的 PI 分支承载）

/// Java 类锚点：`PINodeModel`（Rust 侧由 `XmlNode` 的 PI 分支承载）
#[allow(dead_code)]
pub(crate) struct PiNodeModel;
