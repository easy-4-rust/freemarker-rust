//! 字符数据节点模型 —— 对应 Java `freemarker.ext.dom.CharacterDataNodeModel`
//! （文本/注释等字符数据节点的模型；Rust 侧由 `XmlNode` 的 text/comment 分支承载）

/// Java 类锚点：`CharacterDataNodeModel`（Rust 侧由 `XmlNode` 的 text/comment 分支承载）
#[allow(dead_code)]
pub(crate) struct CharacterDataNodeModel;
