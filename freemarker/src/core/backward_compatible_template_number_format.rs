//! 向后兼容数字格式 —— 对应 Java `freemarker.core.BackwardCompatibleTemplateNumberFormat`
//! （ICI 向后兼容模式的数字格式；Rust 侧由 `builtins::format` 模块承载）

/// Java 类锚点：`BackwardCompatibleTemplateNumberFormat`（Rust 侧由 `builtins::format` 模块承载）
#[allow(dead_code)]
pub(crate) struct BackwardCompatibleTemplateNumberFormat;
