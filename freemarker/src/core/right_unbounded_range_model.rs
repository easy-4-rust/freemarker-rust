//! 右无界范围模型抽象基类 —— 对应 Java `freemarker.core.RightUnboundedRangeModel`
//! （右无界范围的公共基类；Rust 侧由 `listable_right_unbounded_range_model`/
//!  `nonlistable_right_unbounded_range_model` 承载）

/// Java 抽象类锚点：`RightUnboundedRangeModel`
/// （Rust 侧由 `listable_right_unbounded_range_model`/
///  `nonlistable_right_unbounded_range_model` 承载）
#[allow(dead_code)]
pub(crate) struct RightUnboundedRangeModel;
