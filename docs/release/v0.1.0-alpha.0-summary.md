# freemarker-rust 生产就绪第一轮交付总结（v0.1.0-alpha.0）

> 提交 tag：`v0.1.0-alpha.0`（2026-08-03）；详见 [CHANGELOG.md](CHANGELOG.md) v0.1.0-alpha.0 条目。

## 1. 阶段交付（按 plan 阶段 A → B → C → D）

| 阶段 | 交付 | 状态 |
|---|---|---|
| A1 依赖与许可治理 | `deny.toml`（17 类 SPDX 表达式 allow 列表 + sources crates.io + advisories 豁免机制）+ `.cargo/audit.toml`（占位空——当前 0 项豁免，audit 0 error） | ✅ |
| A2 workspace 元数据 | 仓根 `[workspace.package]` 补 description/authors/repository/homepage/categories/keywords；各成员 crate categories/keywords/repository 显式（freemarker-pyo3 metadata 位置 bug 修复） | ✅ |
| A3 公共 API 基线 | `docs/release/api-baseline.txt` 3,705 项；CI `cargo public-api` diff 门禁 | ✅ |
| A4 版本治理文档 | `docs/release/versioning.md`（alpha→1.0 晋级规则与可执行门禁清单） | ✅ |
| A5 release workflow | `.github/workflows/release.yml`（tag v* 触发 `cargo publish --dry-run` + GitHub Release + CHANGELOG 提取）+ `docs/release/publishing.md` | ✅ |
| A6 多 OS CI 矩阵 | test job 矩阵 ubuntu/macos/windows × stable；MSRV 1.85 独立 job | ✅ |
| A7 docs.rs 元数据 | `freemarker/Cargo.toml` `[package.metadata.docs.rs]` all-features + rustdoc-args | ✅ |
| A8 鲁棒性 + 安全 | `freemarker-test/tests/robustness_fuzz_smoke.rs`（1024 用例无 panic）+ `freemarker/benches/simple_render.rs`（5 指标）+ `docs/release/benchmarks.md` + `freemarker-test/tests/security_smoke.rs`（7 边界）+ `docs/release/security.md` | ✅ |
| B BLOCKED 5→0 | output-encoding2/3（启用 `transcode_output`）+ number-literal（解析器 jython25 旧式语法）+ bean-maps（Rust fixture 路由）+ identifier-escaping（`?sort` 字符串排序对齐）+ 1 项 transforms 文档登记 `NOT_APPLICABLE.md` | ✅ |
| C1 安全测试套件 | 7 个边界测试（`?api`/`?new`/输出编码/unwrap/ICI） | ✅ |
| C2 cargo package 演练 + README | `cargo package -p freemarker` 144 files / 1.3MiB 通过；根 `README.md` | ✅ |
| C3 全门禁复测 | fmt/clippy/test/golden/fuzz/criterion/deny/audit/public-api 全部绿 | ✅ |
| D1-D3 CHANGELOG + 落档 | `CHANGELOG.md` 0.1.0-alpha.0 条目 + tag v0.1.0-alpha.0 + 本总结 | ✅ |

## 2. 验证结果（最终复测 C3 实际数值）

| 门禁 | 数值 |
|---|---|
| `cargo fmt --all --check` | 绿 |
| `cargo clippy --workspace --all-targets -- -D warnings` | 0 error |
| `cargo test --workspace --exclude freemarker-pyo3` | 317 passed / 1 FAILED（macros2 预存在回归） |
| `cargo test -p freemarker-test --test golden` | 25/25 硬编码 PASS；runner 跑 89/128 PASS（+7） |
| `cargo test -p freemarker-test --test security_smoke` | 7/7 边界 PASS |
| `cargo test -p freemarker-test --test robustness_fuzz_smoke` | 2/2 fuzz PASS（1024 用例） |
| `cargo deny check` | 全绿（advisories/bans/licenses/sources） |
| `cargo audit` | 0 error |
| `cargo public-api -p freemarker` diff `docs/release/api-baseline.txt` | 0 diff |
| `cargo package -p freemarker` | 144 files / 1.3MiB 演练通过 |

## 3. golden 迁移测试（验收报告 v12）

| 维度 | 数值 |
|---|---:|
| MIRRORED | 87（+5：本轮 BLOCKED 修复 + 文档同步） |
| NOT_APPLICABLE | 41（决策 1：JVM 反射 33 项不可恢复 + transforms 1 项补登记） |
| BLOCKED | 0（5→0） |
| FAIL | 1（macros2 预存在回归——后续迭代 B6） |

## 4. 本轮"未完成"（明确范围外）

- **macros2 预存在回归**：宏嵌套求值 "c null or missing"——属宏内参数解析边界，与本轮 BLOCKED 修复无关；已登记为后续迭代 B6
- **freemarker-pyo3 不发布到 crates.io/PyPI**（决策 2）：本轮仅完成元数据登记
- **cargo-fuzz 长期 fuzz 目标**：留文档，本轮用 proptest 替代
- **JVM 反射 33 项 NOT_APPLICABLE 永久保留**（决策 1：POJO 反射 25 + `?api` 2 + `?new` 4 + XML 节点 2）
- **输出格式（RTF/CSS/JSON/JavaScript/PlainText）**：v1 P4 TODO
- **Servlet/JSP 集成**：v1 不在范围

## 5. 与 thymeleaf-rust 治理对位（按 plan 阶段 0-5 同口径）

| 治理门禁 | thymeleaf-rust | freemarker-rust（本轮后） |
|---|---|---|
| deny/audit | ✅ | ✅ |
| 公共 API 基线 + diff 门禁 | ✅ | ✅ |
| 多 OS 矩阵（ubuntu/macos/windows + MSRV） | ✅ | ✅ |
| docs.rs 元数据 | ✅ | ✅ |
| proptest fuzz | ✅ | ✅（解析 + 渲染 smoke） |
| criterion 基准 | ✅ | ✅（5 指标落档） |
| 安全/边界测试 | ✅ | ✅（7 边界） |
| 严格审计 0/0/0 | ✅ | 不适用（freemarker 无 Java→Rust 对象级审计——以 golden 87/128 MIRRORED 替代） |
| CHANGELOG + versioning.md + publishing.md | ✅ | ✅ |
| release workflow | ✅ | ✅（cargo publish --dry-run + GitHub Release） |
| **JVM 反射** | 不需要（Thymeleaf 表达式走白名单 + ACL） | **受限子集**（决策 1：freemarker `BeansWrapper` 不可 1:1 实现，33 项 NOT_APPLICABLE 永久） |

## 6. 下一轮（v0.1.0 → 0.1.0-stable / 1.0）路线

按 `docs/release/versioning.md` §3 晋级规则：
1. **A9**（cargo-fuzz 长期目标 + 二进制大小/SIMD 优化）
2. **B6**（macros2 预存在回归修复）
3. **support 整合增强**：hyper 10 测试 + pyo3 crates.io 发布 + 13 框架适配合同测试补全
4. **D4**（crates.io 实际 `cargo publish -p freemarker`）
5. **C4**（golden → java_ported 全量 128 MIRRORED，0 FAIL 收尾）
